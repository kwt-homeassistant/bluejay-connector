# 规范执行检查清单

## 运行元信息

- 日期：2026-08-01
- 范围：AITO 最近 365 日历史行程、响应兼容、M8 AC/DC 充电遥测修复与已授权正式部署
- 风险等级：源码 L2；正式 HA 替换与 Core 重启 L3
- 规则来源：工作区 `AGENT.md`、`.codex/delivery.config.yml`、delivery-autopilot baseline

## 变更前

- [x] 目标、验收标准、边界和非目标已明确。
- [x] 正式 HA、账号和车辆动作已划为人工闸门。
- [x] 真实截图与行程数据禁止进入公开仓库。

## 架构与安全

- [x] API、解析、coordinator 和 entity 职责分离。
- [x] 完整记录不进入实体 attributes、诊断或日志。
- [x] 失败不伪装为新的 `0 km`。
- [x] 输入边界、时区和非法响应测试完成。
- [x] 空窗口覆盖、同日新值覆盖、365 日完成、中间缺口优先和断点续传测试完成。
- [x] 私有存储序列化不包含 `tripId`、坐标或未知字段。
- [x] 充电状态使用 App 已确认的 `0/1/5/6/7/18/25` 枚举并取 AC/DC 最大值。
- [x] AC、DC、双通道、非充电清零和旧通用字段回退均有合成测试。
- [x] 仓库名称不含品牌、车型、车辆或 HA 关键词。
- [x] 识别性 Topics 已清空，Custom Property 分类仍存在。
- [x] 明确记录改名不隐藏 PUBLIC 可见性、fork 关系或代码搜索结果。

## 验证证据

- [x] `git diff --check`
- [x] Python 3.12 编译
- [x] 24 项单元测试
- [x] JSON 校验
- [x] Gitleaks 最终敏感信息审计

## 文档与交付

- [x] README、预发布报告和实现反馈已更新。
- [x] 一年行程功能已单独提交并 push。
- [x] 新仓库 URL 和根仓库主设计、运行记录、变更日志已同步并 push。
- [x] 本轮不包含任何车辆控制、账号权限变更或原始响应导出。
- [x] `0.4.2` 正式部署、Core 检查/重启、45 个实体、非充电清零和原状态回归通过。
- [x] 临时 SSH 公钥已恢复，`authorized_keys=0`。
- [ ] 正在充电时的非零电流/功率仍待下一次充电窗口验收。
- [ ] 私有 Store 与历史实体仍未就绪；脱敏结构标记为 `null`，需要另行修正历史数据源或请求契约。
