# 预发布报告

## 目标

- 环境：本地 fork，分支 `codex/energy-history-foundation`
- 版本：AITO `0.4.1` 候选
- 部署目标：已授权替换正式 Home Assistant 中历史解析失败的 `0.4.0`

## 验证结果

- `diff_check`：`git diff --check` 通过。
- `build_or_compile`：Python 3.12 对 `custom_components` 和 `tests` 编译通过。
- `unit`：17 项合成单元测试通过，覆盖顶层行程数组、原生桥接包装、日志脱敏、持久化往返、窗口合并、空窗口水位、7 日聚合、一年完成和中间缺口。
- `domain_specific`：manifest、英文 strings、简体中文翻译和 `hacs.json` JSON 校验通过。
- 隐私：Gitleaks 最终扫描约 613.68 KB，无泄漏发现；公开 fixture 使用 2099 年合成数据。

## 未执行项

- Hassfest/HACS Action：现有 GitHub OAuth 凭据缺少 `workflow` scope，新增工作流无法推送，因此本轮不扩大账号权限并暂不纳入；HACS 元数据保留，远端自动校验待后续单独授权补充。
- `0.4.1` 正式运行时：待本候选提交固定后部署验收。
- 真实响应只由正式 HA 内解析和私有持久化，不导出、不打印、不写入公开 fixture。

## 已知问题

- 接口未公开，后续可能漂移；结构失败时保留旧缓存。
- 正式运行仍存在既有 APIG TLS 和 `TrackerEntity` 弃用风险，本轮未解决。
- 远端仓库已改为 `kwt-homeassistant/bluejay-connector` 并清空 Topics；PUBLIC 可见性和 fork 上游关系未改变，不能作为隐私或反发现保证。
