# 发布后验证

## 当前状态

- 未创建 GitHub Release。正式 HA 已部署 `0.4.0`；原车辆状态正常，但历史解析因顶层数组契约未兼容而失败。
- `0.4.1` 候选已完成本地验证，本文档待正式替换后追加实体、Store、回填和原状态回归结果。
- 远端仓库当前为 `kwt-homeassistant/bluejay-connector`；Topics 为空，Custom Property `component_type=custom_integration` 保留，仓库仍为 PUBLIC fork。

## 后续正式验收

- 经单独授权补充 Actions 工作流后，远端 Hassfest/HACS/单元测试全部通过。
- 升级前完成备份、差异审查和回滚准备。
- 用真实但不输出的响应核对跨月 7 日范围、30 日回填窗口、时区、空行程和请求失败恢复。
- 核对 8 个实体的状态、更新时间、Recorder 行为、365 日覆盖、reload/unload 和 HA 重启断点恢复。
- 只检查私有 Store 的结构和权限，不展示车辆 ID、日期、里程、速度、能耗或完整内容。
