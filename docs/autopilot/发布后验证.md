# 发布后验证

## 当前状态

- 未创建 GitHub Release。`0.4.2@7a2d8ab` 已在正式 HA 加载，组件备份为 `/config/.codex-backups/aito-pre-0.4.2-20260801`。
- 首次发布尝试发现车辆在替换前已经结束充电，严格的“充电中”验收未满足，脚本按预期自动恢复 `0.4.0`；确认这不是组件加载失败后，再次按非充电一致性闸门发布 `0.4.2`。
- Core `2026.7.4` 配置检查和重启通过；配置条目 `loaded`，manifest 为 `0.4.2`，45 个实体均完成状态回读，临时 SSH 公钥恢复为 0。
- 当前车辆未充电，正式环境确认充电状态为“未充电”、电流 `0 A`、功率 `0 kW`；车辆在部署前已结束充电，因此非零电流和功率仍不能标记为真实充电验收。
- 8 个历史实体仍未就绪，覆盖和进度为 `unknown`，私有 Store 未创建。日志仅有 1 个契约失配和 1 个回填暂停类别；脱敏结构标记为 `null`，未输出原始响应、日期、里程、车辆 ID 或坐标。
- 远端仓库当前为 `kwt-homeassistant/bluejay-connector`；Topics 为空，Custom Property `component_type=custom_integration` 保留，仓库仍为 PUBLIC fork。

## 健康检查

- Core 检查、重启和 HTTP 恢复：通过。
- AITO 配置条目、版本和实体注册：通过。
- 非充电状态、电流和功率一致性：通过。
- 正在充电的 AC/DC 通道：未验证，原因是车辆在发布前已结束充电。
- 临时 SSH 公钥清理：通过，`authorized_keys=0`。
- 车辆控制：未调用。

## 事故 / 回滚

- 是否触发：yes。
- 原因：首次尝试坚持“正在充电”验收，但部署前基线已经变为 `0 V`、剩余时间 `0`，无法观察活动充电链路。
- 回滚结果：自动恢复 `0.4.0`、Core 检查/重启和临时密钥清理均通过；随后使用不降低真实性要求的非充电一致性闸门重新发布 `0.4.2`。

## 后续正式验收

- 经单独授权补充 Actions 工作流后，远端 Hassfest/HACS/单元测试全部通过。
- 升级前完成备份、差异审查和回滚准备。
- 用真实但不输出的响应核对跨月 7 日范围、30 日回填窗口、时区、空行程和请求失败恢复。
- 核对 8 个实体的状态、更新时间、Recorder 行为、365 日覆盖、reload/unload 和 HA 重启断点恢复。
- 在车辆仍充电时核对状态为“充电中”、电流大于 `0 A`、功率与电压乘电流一致；若已停止充电，只能验证非充电清零，不能把它写成充电路径验收。
- 历史接口按 `shape=null` 重新检查请求契约、鉴权和 App 原生桥接差异；不得继续把问题归因于数组包装解析。
- 只检查私有 Store 的结构和权限，不展示车辆 ID、日期、里程、速度、能耗或完整内容。
